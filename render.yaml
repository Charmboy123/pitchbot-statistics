services:
  - type: web
    name: elite-football-ai
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    # ... rest of your envVars unchanged
